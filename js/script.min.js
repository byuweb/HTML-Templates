function activateMenus() {
    $("#header-top").append('<a href="#" class="menu-button">Menu</a>'), $("#header-top").delegate(".menu-button", "click", function () {
        $("body").toggleClass("sideNav")
    }), $("nav li:has(.mega, .sub) > a").click(function (e) {
        e.preventDefault();
        var t = $(this).parent();
        return t.hasClass("hover") && clickOpened ? t.removeClass("hover") : (t.addClass("hover"), $("nav li").not(t).removeClass("hover"), clickOpened = !0), !1
    }), $("nav li:has(.mega, .sub)").click(function (e) {
        e.stopPropagation()
    });
    var e = {
        sensitivity: 10,
        interval: 75,
        over: rollOver,
        timeout: 350,
        out: rollOut
    };
    $("nav.no-js").removeClass("no-js"), $("nav li .sub").each(function () {
        var e = $(this),
            t = e.parent().position().left;
        t > e.parent().parent().outerWidth() - e.outerWidth() && e.css("right", 0)
    })
}

function setupNavPosition() {
    $("#main-header").append('<div class="nav-container"></div>'), $("#secondary-nav, #primary-nav").detach().appendTo(".nav-container")
}

function rollOver(e) {
    $(this).hasClass("hover") || (clickOpened = !1, $(this).addClass("hover"), $("nav li").not(this).removeClass("hover"), $(document).click(hideAllMenus))
}

function rollOut() {
    $(this).removeClass("hover")
}

function hideAllMenus() {
    $("nav li").removeClass("hover"), $(document).unbind("click")
}

function endsWith(e, t) {
    return e.indexOf(t, e.length - t.length) !== -1
}

$(function () {
    activateMenus()
});